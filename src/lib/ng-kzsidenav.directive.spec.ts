import { KZSidenavDirective } from './ng-kzsidenav.directive';

describe('KZSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new KZSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
